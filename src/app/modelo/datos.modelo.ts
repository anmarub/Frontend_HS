export class ModeloDatos{
    id?: string; // deben se iguales a los campos de backend - persona.controller metodo post identificarPersona
    nombres?: string; // deben se iguales a los campos de backend - persona.controller metodo post identificarPersona
    correo?: string; // deben se iguales a los campos de backend - persona.controller metodo post identificarPersona
}