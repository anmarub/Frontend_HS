export class ModeloProducto {
    id?: string; //atributos del modelo son iguales a los creados en Backend
    nombre?: string; //atributos del modelo son iguales a los creados en Backend
    precio?: number; //atributos del modelo son iguales a los creados en Backend
    imagen?: string; //atributos del modelo son iguales a los creados en Backend
}